# imdb-sentiment-analysis-tensorflow
This project performs sentiment analysis on movie reviews using a neural network built with TensorFlow. The model is trained on the IMDB Movie Reviews dataset to classify reviews as positive or negative. It includes text preprocessing, vocabulary creation, encoding, model training, and evaluation to understand sentiment in text data.
